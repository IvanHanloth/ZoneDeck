# Windows 平台配置
[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"

# Linux 平台配置 (需要先通过系统包管理器安装 mold，例如 apt install mold)
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
