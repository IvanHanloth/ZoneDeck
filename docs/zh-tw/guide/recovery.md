---
title: 視窗復原與當機防護
---

# 視窗復原與當機防護

隱藏視窗本質上是把視窗設為不可見。萬一出現意外（忘了快速鍵、核心異常結束等），視窗會不會「永遠消失」？Boss Key 從**手動工具**和**自動防線**兩方面確保這不會發生。

## 視窗復原工具

**視窗復原工具**用於找回被誤隱藏、又無法透過快速鍵復原的視窗。

![視窗復原工具](/static/recovery-tool.png)

開啟方式：**設定介面 → 其他選項 → 工具 → 視窗復原工具** 或 **通知區域選單 → 視窗復原工具**。

工具會列出系統中的所有視窗（包括目前不可見的），您只需**勾選**想找回的視窗，按一下「顯示視窗」即可讓它重新顯示。

視窗復原工具也支援將視窗**凍結／解除凍結**，凍結後視窗會被暫停算圖，降低資源消耗。凍結的視窗仍然可透過復原工具找回。

## 三層防線

Boss Key 核心內建了**當機自癒三層防線**，即使程式意外當機也能盡量保證視窗安全。

### 第一層：當機記錄

核心會把關鍵事件與 panic 資訊寫入程式資料夾 `logs` 資料夾下的記錄檔，按日切割為 `BossKey-YYYY-MM-DD.log`，並依 [記錄檔保留天數](/zh-tw/guide/options) 自動清除過期檔案（設為關閉則不記錄）。**排查問題時先看當天的記錄檔。**

### 第二層：當機復原

每次隱藏視窗時，核心會把「隱藏了哪些視窗、凍結／靜音了哪些程序」寫入 `recovery.json`；正常顯示或結束時刪除該檔案。

核心重新啟動時若發現該檔案仍然存在，表示上次是**異常結束**——於是它會自動**找回視窗、解除凍結程序、取消靜音**。

### 第三層：排程工作

[開機自動啟動](/zh-tw/guide/autostart) 若以排程工作方式註冊，會帶有失敗自動重新啟動策略（當機後約 1 分鐘內自動重新啟動，最多 3 次）。核心當機 → 排程工作重新啟動核心 → 重新啟動後觸發第二層防線復原視窗，形成閉環。

## 相關檔案一覽

| 檔案 | 位置 | 作用 |
| --- | --- | --- |
| `config.json` | 程式資料夾 | 您的全部設定與綁定 |
| `logs/BossKey-YYYY-MM-DD.log` | 程式資料夾 | 當機／事件記錄（按日切割，依保留天數自動清除） |
| `recovery.json` | 程式資料夾 | 隱藏狀態快照，用於當機復原（正常結束時刪除） |

::: warning 請勿手動刪除執行中的 recovery.json
若在視窗處於隱藏狀態時手動刪除 `recovery.json`，當機復原將失去這次快照。
:::
