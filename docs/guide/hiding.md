---
title: 隐藏设置
---

# 隐藏设置

配置界面的 **隐藏设置** 标签页集中了执行隐藏时的附加行为：静音、暂停键、托盘图标与最小化。触发方式请查看[热键与鼠标手势](/guide/hotkeys)，进程冻结与内存清理请查看[进程冻结](/guide/freeze)（设置位于 **能效控制** 标签页），托盘图标是否显示与点击它做什么请查看[提示设置](/guide/notifications#显示托盘图标)。

## 隐藏窗口后静音

隐藏窗口时**静音目标进程的音频**，恢复显示时自动取消静音。适合在隐藏正在播放声音的视频 / 音乐窗口时。

- 默认**开启**。
- 基于 Windows Core Audio 会话级静音，仅静音目标进程，不影响系统其他声音。

## 同时隐藏当前活动窗口

开启后，按下隐藏热键时，除已绑定的窗口外，还会**同时隐藏当前正在激活的前台窗口**。

- 默认**开启**。
- 适合"临时正在看的窗口没有提前绑定"的场景。

## 同时隐藏 ZoneDeck 托盘图标

开启后，隐藏窗口时会**连 ZoneDeck 自身的托盘图标一起隐藏**，更加隐蔽；再次触发恢复即可恢复窗口与图标。

- 默认**关闭**。
- 该选项只作用于 ZoneDeck 自己的托盘图标，**不会隐藏其他程序的托盘图标**（包括被隐藏窗口所属程序的图标）。
- 若你想让图标**一直不显示**，用[显示托盘图标](/guide/notifications#显示托盘图标)开关；本项只在隐藏期间生效。

::: warning 
托盘图标被隐藏后，你将无法通过点击图标来恢复或打开设置。请务必记住你的**隐藏 / 显示热键**或鼠标手势，用它来恢复。
:::

::: tip 隐藏其他程序的托盘图标
Windows 自带控制托盘图标显隐的功能，可手动设置哪些程序的图标显示在任务栏角落，具体步骤参见微软官方教程 [在 Windows 中自定义任务栏 · 系统托盘](https://support.microsoft.com/zh-cn/windows/experience/personalization/customize-the-taskbar-in-windows#system-tray)，或直接打开 [任务栏设置](ms-settings:taskbar)（`ms-settings:taskbar` 链接仅在 Windows 上有效，浏览器会先请求确认）。
:::

## 隐藏前发送暂停键

开启后，隐藏窗口**前**会先向窗口发送**媒体暂停键**，尝试暂停其中正在播放的视频 / 音乐。

- 默认**关闭**。
- 与"隐藏后静音"侧重点不同：静音只是消音，暂停键会真正暂停播放。
- 除了此功能外，ZoneDeck 提供的**进程冻结**功能，也有相同的暂停效果，同时还能降低资源消耗，详见 [进程冻结](/guide/freeze)。

## 隐藏前先最小化窗口

开启后，隐藏窗口**前**会先把它**最小化**，恢复时再还原成隐藏前的大小。

- 默认**关闭**。
- 隐藏前是**最大化**的窗口，恢复后仍是最大化，不会被还原成普通大小。
- 隐藏前**就已最小化**的窗口保持最小化——ZoneDeck 没有替你最小化它，恢复时自然也不替你还原。
- 某些程序在收到最小化消息时会释放一部分资源（如暂停渲染、收起到托盘），配合[进程冻结](/guide/freeze)使用效果更明显。

::: tip 隐藏与恢复是一次成对的切换
ZoneDeck 的"恢复"只逆转**它自己造成的**改变：没被它最小化的窗口不会被还原大小，没被它隐藏的窗口（程序自己藏起来的）也不会被弹出来。但静音、冻结这类副作用仍会照常施加与撤销。
:::

## See Also

[在 Windows 中自定义任务栏 - Microsoft 支持](https://support.microsoft.com/zh-cn/windows/experience/personalization/customize-the-taskbar-in-windows#system-tray)
