root = true

# 只管 Markdown 的自动折行，其余文件类型交给各自的工具链（rustfmt / prettier）。
# 编辑器按行宽折行会把 ** 拆到两行，强调随之失效，网页上渲染出裸的 **。
[*.md]
max_line_length = off
ij_markdown_wrap_text_if_long = false
ij_markdown_wrap_text_inside_blocks = false
